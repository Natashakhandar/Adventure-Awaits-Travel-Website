version: '3'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"

  frontend:
    image: node:18
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "cd frontend && npm install && npm run dev -- --host 0.0.0.0"
    ports:
      - "5173:5173"
